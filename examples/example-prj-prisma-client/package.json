{
  "private": true,
  "name": "example-prj-prisma-client",
  "version": "1.8.2",
  "scripts": {
    "bootstrap": "npm run generate",
    "migrate:test:ci": "DATABASE_URL=\"file:./test.db\" prisma migrate dev",
    "generate": "prisma generate",
    "test": "DATABASE_URL=\"file:./test.db\" jest",
    "test:ci": "DATABASE_URL=\"file:./test.db\" jest"
  },
  "devDependencies": {
    "@quramy/jest-prisma": "*",
    "@quramy/jest-prisma-node": "*",
    "@prisma/client": "*",
    "@types/jest": "30.0.0",
    "prisma": "*",
    "jest": "30.2.0",
    "ts-jest": "29.4.6"
  },
  "dependencies": {
    "sqlite": "4.1.2"
  }
}
